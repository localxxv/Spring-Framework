spring:
  profiles:
    active: ${APP_PROFILE:jpa}
  main:
    banner-mode: off

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET:my-super-secret-jwt-key-my-super-secret-jwt-key-123456}
  expiration: ${JWT_EXPIRATION:3600000}